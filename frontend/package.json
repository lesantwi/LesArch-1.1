{
  "name": "lesarch",
  "version": "2.0.0",
  "description": "LesArch — Mapping Archaeological Wonders. Interactive map, 3D reconstructions, open research data.",
  "type": "module",
  "scripts": {
    "start": "astro dev --host 0.0.0.0 --port 3000",
    "dev": "astro dev --host 0.0.0.0 --port 3000",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 3000",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0",
    "typescript": "^5.6.3"
  }
}
